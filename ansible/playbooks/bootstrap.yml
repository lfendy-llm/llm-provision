---
- name: Bootstrap Ubuntu Server
  hosts: local
  become: yes
  gather_facts: yes

  tasks:
    - name: Include apt upgrade tasks
      ansible.builtin.import_tasks: ../tasks/bootstrap/apt_upgrade.yml

    - name: Include user setup tasks
      ansible.builtin.import_tasks: ../tasks/bootstrap/create_devuser.yml

