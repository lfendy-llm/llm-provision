---
- name: Ensure system packages are up-to-date
  apt:
    update_cache: yes
    upgrade: dist
    autoremove: yes
    cache_valid_time: 3600

- name: Ensure zsh is present
  apt:
    name: zsh
    state: present
